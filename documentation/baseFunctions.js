
/**
 * Valida el tipo de configuracion. Valida que debe de existir la misma distancia entre los vertices de una
 * configuracion. Ejemplo: Triangular, Cuadrada, Mixta.
 *
 * @param      int   configType  The configuration type
 * @return     boolean
 */
function validConfigType(configType) { 
	return true;
};

function validMinSensors()


Cuadrantes son logicos.
	- se forman a partir de el despliegue de los sensores 
	y contienen regiones basados en puntos cardinales.

